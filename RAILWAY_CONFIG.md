# Конфигурация для Railway

## ✅ Рекомендуемая конфигурация (Gemini 2.5 Flash)

### Обязательные переменные:
```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
GEMINI_API_KEY=ваш_gemini_ключ
```

### Опциональные переменные (можно не устанавливать):
```
# Эти переменные имеют правильные значения по умолчанию:
USE_GEMINI_PRIMARY=true  # Можно не устанавливать
GEMINI_MODEL=gemini-2.5-flash  # Можно не устанавливать
```

### ⚠️ ВАЖНО - НЕ устанавливайте эти переменные (или установите в false):
```
USE_OPENROUTER_PRIMARY=false  # Или удалите эту переменную
```

---

## 🚀 Пошаговая настройка на Railway

### Шаг 1: Получить API ключи

1. **Telegram Bot Token:**
   - Зайдите в [@BotFather](https://t.me/BotFather)
   - Создайте бота: `/newbot`
   - Скопируйте токен

2. **Gemini API Key (бесплатный):**
   - Зайдите на: https://aistudio.google.com/app/apikey
   - Нажмите "Create API Key"
   - Скопируйте ключ

### Шаг 2: Настроить переменные в Railway

1. Откройте ваш проект на Railway
2. Перейдите в раздел **"Variables"**
3. Добавьте переменные:

```
TELEGRAM_BOT_TOKEN = ваш_токен_от_botfather
GEMINI_API_KEY = ваш_ключ_от_google
```

4. **УДАЛИТЕ** переменную `USE_OPENROUTER_PRIMARY` если она есть
5. **УДАЛИТЕ** переменную `OPENROUTER_API_KEY` если не планируете использовать OpenRouter

### Шаг 3: Перезапустить деплой

1. Нажмите **"Deploy"** → **"Redeploy"**
2. Дождитесь завершения деплоя
3. Проверьте логи - должно быть:
   ```
   ✅ Gemini API Key найден (основной провайдер)
      → Model: gemini-2.5-flash (Gemini 2.5 Flash)
      → Vision: gemini-2.5-flash
   ```

---

## 📊 Текущая конфигурация моделей

### Primary: Gemini 2.5 Flash
- **Качество русского:** ⭐⭐⭐⭐⭐ Отлично
- **Скорость:** ⚡⚡⚡ Очень быстро
- **Контекст:** 2M токенов
- **Стоимость:** FREE (1M tokens/min бесплатно)
- **Vision:** Gemini 2.5 Flash (мультимодальность)

### Fallback: Groq (если установлен GROQ_API_KEY)
- Llama 3.1 405B
- Llama 3.3 70B

---

## 🔧 Дополнительные настройки (опционально)

### Если хотите использовать Groq как fallback:

1. Получите бесплатный ключ: https://console.groq.com/keys
2. Добавьте в Railway Variables:
   ```
   GROQ_API_KEY=ваш_groq_ключ
   ```

### Если хотите использовать OpenRouter (для Kimi K2 с 256K контекстом):

1. Получите ключ: https://openrouter.ai/keys
2. Настройте privacy: https://openrouter.ai/settings/privacy
   - Включите "Allow Free Models"
3. Добавьте в Railway Variables:
   ```
   OPENROUTER_API_KEY=ваш_openrouter_ключ
   USE_OPENROUTER_PRIMARY=true
   ```

---

## 🐛 Решение проблем

### Ошибка "User not found" (401):
- **Причина:** Установлен `USE_OPENROUTER_PRIMARY=true` но нет `OPENROUTER_API_KEY`
- **Решение:** Удалите `USE_OPENROUTER_PRIMARY` из Railway Variables

### Ошибка "No endpoints found matching your data policy" (404):
- **Причина:** В OpenRouter не разрешены бесплатные модели
- **Решение:**
  1. Зайдите на https://openrouter.ai/settings/privacy
  2. Включите "Allow Free Models"
  3. ИЛИ: Удалите `OPENROUTER_API_KEY` и используйте только Gemini

### Бот не отвечает:
1. Проверьте логи Railway
2. Убедитесь что установлен `GEMINI_API_KEY`
3. Проверьте что `USE_GEMINI_PRIMARY` не установлен в `false`

### Проверить текущую конфигурацию:
Посмотрите в логи Railway при запуске - должно быть:
```
✅ Gemini API Key найден (основной провайдер)
   → Model: gemini-2.5-flash (Gemini 2.5 Flash)
   → Vision: gemini-2.5-flash
```

---

## 📝 Минимальная рабочая конфигурация

Для работы бота достаточно **ТОЛЬКО 2 переменных**:

```
TELEGRAM_BOT_TOKEN=...
GEMINI_API_KEY=...
```

Всё остальное - опционально!

---

## 🎯 Итоговый чеклист

- [ ] Получен Telegram Bot Token от @BotFather
- [ ] Получен Gemini API Key от Google AI Studio
- [ ] Добавлены переменные `TELEGRAM_BOT_TOKEN` и `GEMINI_API_KEY` в Railway
- [ ] УДАЛЕНА переменная `USE_OPENROUTER_PRIMARY` (или установлена в `false`)
- [ ] Выполнен Redeploy
- [ ] В логах видно "✅ Gemini API Key найден"
- [ ] Бот отвечает на сообщения в Telegram

**Готово!** 🎉
