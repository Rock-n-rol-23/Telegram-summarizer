Цели апдейта

Ввести двухпроходную «качество-сначала» суммаризацию с гарантиями сохранения чисел/дат/валют/имен.

Улучшить извлечение контента (web/таблицы, YouTube субтитры, таймштампы).

Централизовать промпты и ввести JSON-режим первой фазы.

Добавить quality-чеки и тесты.

Упростить структуру и выключить локальные модели.

Задачи (пошагово)
1) Структура и конфигурация

Создай каталоги/файлы:

summarization/pipeline.py

summarization/fact_extractor.py

quality/quality_checks.py

prompts/llm_summarize.json5 (или .txt — см. ниже)

В config.py:

Добавь флаги:

ENABLE_LOCAL_FALLBACK=False (по умолчанию),

YT_MAX_DURATION_SECONDS=7200 (или оставь 3600 и поправь тексты приветствия/README**)**,

FORCE_JSON_MODE=True (первая фаза LLM).

Удали/задизейбли transformers-fallback в summarizer.py:

не импортировать и не инициализировать локальные модели,

удалить/закрыть код путей ai-forever/rugpt3... и _initialize_local_model,

статус local_available всегда False.

2) Извлечение фактов (до LLM)

summarization/fact_extractor.py:

Функция extract_key_facts(text: str, lang: Literal["ru","en"]) -> dict:

Вытащить все числа (целые/десятичные, интервалы, проценты, валюты ₽/$/€/слова-денежные), даты/сроки (через dateparser), единицы (шт., млн, млрд, мин/ч/дн).

Собрать предложения-носители (index, text).

Вытянуть основные NER (Natasha RU; spaCy EN small).

Вернуть структуру:

{
  "sentences_with_numbers": [ { "i": 12, "text": "...", "numbers": [{"raw":"38%", "norm":0.38, "unit":"%"}] }, ... ],
  "money": [{"raw":"3 млрд ₽","norm":3000000000,"currency":"RUB","sentence_i":7}],
  "dates": [{"raw":"к 15 сентября","norm":"2025-09-15"}],
  "entities": {"ORG":[...], "PERSON":[...], "GPE":[...]}
}


Функция select_must_keep_sentences(...) -> set[int] — индексы предложений, которые обязаны попасть в итог.

В summarizers/text_summarizer.py:

Добавь scoring-мультипликатор для предложений с числами/датами (+α к весу TextRank).

В create_extract_summary(...) всегда добавляй раздел «🔢 Цифры и факты» (список), даже без LLM.

3) LLM «двухпроходка» и JSON-режим

Положи файл prompts/llm_summarize.txt cо строгими инструкциями:

SYSTEM (всегда):
«Ты — педантичный редактор-саммаризатор. Ничего не выдумывай. Сохраняй все числа, даты, проценты, денежные суммы, диапазоны, единицы измерения и имена. Если данных нет — пиши not specified. Сначала сформируй JSON с полями:

bullets: список из 5–12 коротких маркеров сути,

key_facts: массив объектов {value_raw, value_norm?, unit?, qualifier?, sentence_i?, timestamp?},

entities: {ORG, PERSON, GPE},

uncertainties: массив строк, где источник неполон или двусмысленен.
Никакого текста вне JSON.»

USER (для фазы A):

LANGUAGE={ru|en}
FORMAT={bullets|paragraph|keywords}
TARGET_CHARS=...
MUST_KEEP_SENTENCE_INDEXES=[...]
TEXT=<<<...чистый текст чанка или полного материала...>>>


Вторая фаза (сборка финального текста) — отдельный промпт:
«Собери чистый читабельный итог в формате FORMAT и языке LANGUAGE. Включи все из key_facts. В конце добавь блок:
🔢 Цифры и факты: — маркированный список value_raw (+контекст). Если есть таймкоды — укажи [mm:ss]. Длина ~TARGET_CHARS. Никакого JSON.»

В summarization/pipeline.py:

Реализуй summarize_text_pipeline(text, lang, target_chars, format):

Разбивка на чанки (заголовки/абзацы, потом предложения; предел 2500–3000 символов).

Для каждого чанка: fact_extractor → LLM (фаза A JSON).

Слияние JSON по чанкам (де-дуп фактов; объединение bullets).

Финальная сборка (фаза B) одним вызовом LLM с агрегированным JSON.

Пост-валидация через quality_checks.validate_numbers_preserved. Если что-то выпало — короткий до-вызов «fixup» (одношаговый), либо добавь отсутствующие факты в конец блока «Цифры и факты».

Если Groq недоступен, делай гибрид: TextRank + must-keep предложения + «Цифры и факты».

4) Web-извлечение (не терять цифры)

В content_extraction/web_extractor.py:

Добавь fallback jusText (рус/англ профили) для отфильтровки «мусора».

Новый помощник extract_tables_as_markdown(html) -> str (через bs4): конвертировать таблицы (<table>) в Markdown (в т.ч. заголовки/ячейки). Прилеплять к основному тексту в конце с разделителем \n\n[Таблицы]\n....

Нормализуй <meta>: title, author, published_at (через dateparser).

В кэш добавь lang и char_count.

5) YouTube-процессинг

В youtube_processor.py:

Добавь зависимость youtube-transcript-api и используй её первой: пытаемся получить RU/EN; если нет — yt_dlp автосабы.

Разбей транскрипт на сегменты 3–5 минут с сохранением [start, end].

Прокинь в фазу A JSON таймштампы; в итоговой сборке добавляй [mm:ss] к фактам.

Приведи лимит длительности: YT_MAX_DURATION_SECONDS из config.py.

Сообщения пользователю изменить, чтобы лимит не противоречил README/приветствию.

6) Quality-чеки и тесты

quality/quality_checks.py:

extract_critical_numbers(text) -> set[str] (регэкспы на %/валюты/диапазоны/даты),

validate_numbers_preserved(src, summary) -> tuple[bool, missing:list[str]],

assert_language(src_lang, out_text); trim_to_length(text, target_chars, tolerance=10%).

Тесты в tests/:

test_numbers_preserved.py: текст с «3 млрд ₽», «38%», «к 15 сентября» → итог обязан содержать все.

test_web_tables_extraction.py: HTML c <table> → Markdown имеется; числа сохранены.

test_youtube_transcript.py: мок транскрипта с таймштампами → в «Цифры и факты» попадает [mm:ss].

test_fallback_no_groq.py: без ключа Groq гибридный вывод с «🔢 Цифры и факты».

7) Интеграция в бота

В simple_bot.py:

Все ветки (текст, URL, YouTube) направлять через summarization/pipeline.summarize_text_pipeline(...).

Сохранить текущие настройки сжатия/формата пользователя.

Для URL: после загрузки из web_extractor добавлять в промпт source_title, source_site, published_at (не публиковать, если пусто).

Для YouTube: в конец ответа добавить «Источник: Название, длительность».

8) requirements.txt

Добавь/обнови минимум:

groq
python-dotenv
aiohttp
beautifulsoup4
lxml
trafilatura
readability-lxml
jusText
dateparser
razdel
natasha
spacy
spacy-langdetect
youtube-transcript-api
yt-dlp
sumy
requests


(Без transformers.)
Инициализацию spaCy EN-модели сделать «ленивой» и безопасной: при отсутствии — падать в чисто регэкспный режим.

Критерии приёмки

Сохранность чисел: для тестовых текстов все ключевые числа/проценты/валюты/даты из источника присутствуют в финальном ответе (или в блоке «🔢 Цифры и факты»).

Web-таблицы: числа из HTML-таблиц не теряются; таблицы присутствуют в Markdown-виде (если есть).

YouTube: транскрипт RU/EN подтягивается без скачивания видео; в саммари проставляются таймштампы [mm:ss] для фактов.

JSON-режим LLM: фаза A всегда возвращает валидный JSON; фаза B никогда не содержит JSON, только читабельный текст.

Нет локальных LLM: зависимости transformers отсутствуют, код не пытается грузить локальные модели.

Лимиты согласованы: ограничение длительности YouTube согласовано в коде, README и приветствии бота.

Тесты: все новые тесты зелёные.

Подсказки по реализации

Регэкспы для чисел: поддержать форматы 1 234, 1,234.56, 3 млрд, 2,5 млн, 38%, +15 б.п., валюты ₽/$/€, «тыс./млн/млрд».

Нормализация дат: dateparser.parse(..., languages=['ru','en']).

Таймштампы: форматировать mm:ss через int(seconds)//60.

Слияние JSON по чанкам: де-дуп по value_raw (нормализуй пробелы) + приоритет более раннего вхождения.

Коротко: явные несоответствия и быстрые фиксы

Лимит YouTube: в коде youtube_processor.py стоит 1 час, а в README/приветствии — «до 2 часов». Привести к одному значению.

Локальный fallback LLM: сейчас присутствует в summarizer.py. Отключить по умолчанию и вычистить тяжёлые зависимости — ты не хочешь локальных моделей.

Промпты разбросаны: вынести в prompts/ и перейти на JSON-первую фазу + финальную сборку — это резко повышает стабильность и повторимость результата.