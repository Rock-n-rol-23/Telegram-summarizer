# Добавление функции суммаризации веб-страниц в существующий Telegram бот

## Задача
Добавить в существующий Telegram бот функциональность автоматической суммаризации веб-страниц по ссылкам, отправленным пользователями.

## 1. Дополнительные зависимости

Добавь в requirements.txt следующие библиотеки:
```
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
validators>=0.22.0
openai>=1.0.0
python-readability>=0.8.0
```

## 2. Основной алгоритм работы

### Шаг 1: Определение URL в сообщении
```python
import re
import validators

def extract_urls_from_message(text):
    """Извлекает все URL из текста сообщения"""
    # Паттерн для поиска URL
    url_pattern = r'http[s]?://(?:[a-zA-Z]|[0-9]|[$-_@.&+]|[!*\\(\\),]|(?:%[0-9a-fA-F][0-9a-fA-F]))+'
    urls = re.findall(url_pattern, text)
    
    # Проверяем валидность каждого URL
    valid_urls = []
    for url in urls:
        if validators.url(url):
            valid_urls.append(url)
    
    return valid_urls
```

### Шаг 2: Извлечение контента веб-страницы
```python
import requests
from bs4 import BeautifulSoup
from readability import Document

def extract_webpage_content(url, timeout=30):
    """Извлекает основной текст с веб-страницы"""
    try:
        # Заголовки для имитации браузера
        headers = {
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36'
        }
        
        # Загрузка страницы
        response = requests.get(url, headers=headers, timeout=timeout)
        response.raise_for_status()
        
        # Проверка размера контента (максимум 5MB)
        if len(response.content) > 5 * 1024 * 1024:
            raise Exception("Файл слишком большой для обработки")
        
        # Извлечение основного контента с помощью readability
        doc = Document(response.content)
        main_content = doc.summary()
        
        # Очистка от HTML тегов
        soup = BeautifulSoup(main_content, 'lxml')
        
        # Удаление ненужных элементов
        for element in soup(['script', 'style', 'nav', 'header', 'footer', 'aside']):
            element.decompose()
        
        # Извлечение текста
        text = soup.get_text()
        
        # Очистка текста
        lines = (line.strip() for line in text.splitlines())
        chunks = (phrase.strip() for line in lines for phrase in line.split("  "))
        text = ' '.join(chunk for chunk in chunks if chunk)
        
        # Получение заголовка страницы
        title = doc.title() or "Без заголовка"
        
        return {
            'title': title,
            'content': text,
            'url': url,
            'success': True
        }
        
    except requests.exceptions.Timeout:
        return {'success': False, 'error': 'Время ожидания истекло'}
    except requests.exceptions.ConnectionError:
        return {'success': False, 'error': 'Не удается подключиться к сайту'}
    except requests.exceptions.HTTPError as e:
        return {'success': False, 'error': f'Ошибка HTTP: {e.response.status_code}'}
    except Exception as e:
        return {'success': False, 'error': f'Ошибка при обработке: {str(e)}'}
```

### Шаг 3: Суммаризация текста через OpenAI API
```python
import openai
import os

# Настройка OpenAI
openai.api_key = os.getenv('OPENAI_API_KEY')

def summarize_text(text, title="", max_length=500):
    """Создает краткое резюме текста используя OpenAI GPT"""
    try:
        # Ограничение длины входного текста (примерно 3000 токенов)
        if len(text) > 12000:
            text = text[:12000] + "..."
        
        # Формирование prompt для суммаризации
        prompt = f"""
Создай краткое резюме следующего текста на русском языке.
Резюме должно быть:
- Не больше {max_length} символов
- Содержать основные идеи и ключевые факты
- Быть понятным и структурированным

Заголовок: {title}

Текст для суммаризации:
{text}

Краткое резюме:"""

        response = openai.chat.completions.create(
            model="gpt-3.5-turbo",
            messages=[
                {"role": "system", "content": "Ты помощник, который создает качественные краткие резюме текстов на русском языке."},
                {"role": "user", "content": prompt}
            ],
            max_tokens=200,
            temperature=0.3
        )
        
        summary = response.choices[0].message.content.strip()
        
        return {
            'summary': summary,
            'success': True
        }
        
    except Exception as e:
        return {
            'success': False,
            'error': f'Ошибка суммаризации: {str(e)}'
        }
```

### Шаг 4: Fallback суммаризация без AI
```python
def simple_text_summary(text, max_sentences=3):
    """Простая суммаризация без AI - берет первые предложения"""
    import re
    
    # Разбивка на предложения
    sentences = re.split(r'[.!?]+', text)
    sentences = [s.strip() for s in sentences if len(s.strip()) > 20]
    
    # Берем первые несколько предложений
    summary_sentences = sentences[:max_sentences]
    summary = '. '.join(summary_sentences)
    
    if len(summary) > 500:
        summary = summary[:500] + "..."
    
    return summary
```

## 3. Интеграция с основным ботом

### Добавь новый обработчик сообщений:
```python
async def handle_url_message(update, context):
    """Обработчик сообщений с URL для суммаризации"""
    message_text = update.message.text
    chat_id = update.message.chat_id
    
    # Извлекаем URL из сообщения
    urls = extract_urls_from_message(message_text)
    
    if not urls:
        return  # Если URL нет, обработчик не срабатывает
    
    # Отправляем сообщение о начале обработки
    processing_msg = await context.bot.send_message(
        chat_id=chat_id,
        text="🔄 Обрабатываю веб-страницу... Это может занять несколько секунд."
    )
    
    for url in urls[:3]:  # Максимум 3 URL за раз
        try:
            # Извлекаем контент
            content_result = extract_webpage_content(url)
            
            if not content_result['success']:
                await context.bot.send_message(
                    chat_id=chat_id,
                    text=f"❌ Ошибка при загрузке {url}:\n{content_result['error']}"
                )
                continue
            
            # Суммаризируем
            summary_result = summarize_text(
                content_result['content'], 
                content_result['title']
            )
            
            if summary_result['success']:
                summary_text = summary_result['summary']
            else:
                # Fallback на простую суммаризацию
                summary_text = simple_text_summary(content_result['content'])
                summary_text += "\n\n⚠️ Использована упрощенная суммаризация"
            
            # Формируем красивый ответ
            response = f"""📄 **Резюме статьи**

🔗 **Источник:** {content_result['title']}
📎 **Ссылка:** {url}

📝 **Основные моменты:**
{summary_text}

📊 **Размер исходного текста:** {len(content_result['content']):,} символов"""

            await context.bot.send_message(
                chat_id=chat_id,
                text=response,
                parse_mode='Markdown',
                disable_web_page_preview=True
            )
            
        except Exception as e:
            await context.bot.send_message(
                chat_id=chat_id,
                text=f"❌ Произошла ошибка при обработке {url}: {str(e)}"
            )
    
    # Удаляем сообщение о процессе обработки
    try:
        await context.bot.delete_message(chat_id=chat_id, message_id=processing_msg.message_id)
    except:
        pass
```

### Регистрация обработчика:
```python
# В главном файле бота, где настраиваются обработчики
from telegram.ext import MessageHandler, filters

# Добавь этот обработчик к существующим
application.add_handler(MessageHandler(
    filters.TEXT & filters.Regex(r'http[s]?://'),
    handle_url_message
))
```

## 4. Настройка переменных окружения

Добавь в файл .env (или в Secrets в Replit):
```
OPENAI_API_KEY=your_openai_api_key_here
```

## 5. Обработка ошибок и ограничений

### Добавь проверки и ограничения:
```python
def is_url_allowed(url):
    """Проверяет, разрешен ли URL для обработки"""
    blocked_domains = [
        'facebook.com', 'instagram.com', 'twitter.com', 
        'youtube.com', 'tiktok.com'  # Социальные сети сложно парсить
    ]
    
    from urllib.parse import urlparse
    domain = urlparse(url).netloc.lower()
    
    for blocked in blocked_domains:
        if blocked in domain:
            return False
    return True

def rate_limit_check(user_id, max_requests_per_hour=10):
    """Простая проверка на спам (можно улучшить)"""
    # Здесь может быть реализация через базу данных или кэш
    # Для простоты возвращаем True
    return True
```

## 6. Улучшения пользовательского опыта

### Добавь команду помощи:
```python
async def help_summarize_command(update, context):
    """Команда помощи по суммаризации"""
    help_text = """
📄 **Суммаризация веб-страниц**

Просто отправьте мне ссылку на статью, новость или веб-страницу, и я создам краткое резюме содержания.

**Поддерживаемые сайты:**
✅ Новостные порталы
✅ Блоги и статьи
✅ Научные публикации
✅ Документация

**Ограничения:**
• Максимум 3 ссылки за раз
• Страницы до 5MB
• Некоторые сайты могут блокировать автоматический доступ

**Пример использования:**
Отправьте: https://habr.com/ru/articles/123456/
Получите: Краткое резюме статьи с ключевыми моментами
"""
    
    await update.message.reply_text(help_text, parse_mode='Markdown')

# Добавь обработчик команды
application.add_handler(CommandHandler("summarize_help", help_summarize_command))
```

## 7. Тестирование функциональности

### Протестируй с различными типами сайтов:
1. **Новостные сайты**: РБК, Интерфакс, Медуза
2. **Технические блоги**: Хабр, VC.ru
3. **Научные статьи**: ArXiv, журналы
4. **Проблемные сайты**: с защитой от ботов

### Проверь обработку ошибок:
- Недоступные сайты (404 ошибки)
- Медленные сайты (timeout)
- Сайты с защитой от ботов
- Некорректные URL

---

**Важные моменты для реализации:**
1. Обязательно добавь API ключ OpenAI в переменные окружения
2. Тестируй на разных сайтах перед запуском
3. Следи за лимитами API OpenAI
4. Добавь логирование для отслеживания ошибок
5. Рассмотри возможность кэширования результатов для популярных статей